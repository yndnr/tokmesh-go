# TK-254902 - 项目结构和基础工具

状态: 待开始
优先级: P0（必须）
创建日期: 2025-12-07
关联设计: DN-254901
预估工时: 3 小时
批次: 1（基础设施）
依赖任务: TK-254901

## 任务目标

搭建项目基础结构，初始化 Go Module，配置 Protobuf 编译环境，实现基础工具函数和测试框架。

## 实现范围

### 1. 项目初始化

#### 1.1 Go Module
```bash
go mod init github.com/tokmesh/tokmesh-go
go mod tidy
```

#### 1.2 依赖管理
```go
// go.mod 关键依赖
require (
    google.golang.org/protobuf v1.31.0
    gopkg.in/yaml.v3 v3.0.1
    github.com/stretchr/testify v1.8.4
)
```

### 2. 目录结构创建

```
tokmesh-go/
├── cmd/
│   └── tokmesh/
│       └── main.go              # 主程序入口
├── internal/
│   └── storage/
│       ├── engine.go            # Engine 主接口（占位）
│       ├── types/               # 数据类型（TK-254901）
│       ├── config/              # 配置（TK-254901）
│       ├── proto/               # Protobuf 定义（TK-254901）
│       ├── memstore/            # 内存存储（TK-254903）
│       ├── index/               # 索引管理（TK-254904）
│       ├── ttl/                 # TTL 管理（TK-254905）
│       ├── wal/                 # WAL（TK-254906）
│       ├── snapshot/            # 快照（TK-254907）
│       ├── dispatcher/          # 调度器（TK-254908）
│       ├── expiry/              # 过期清理（TK-254910）
│       ├── eviction/            # 驱逐（TK-254911）
│       └── disk/                # 磁盘监控（TK-254912）
├── pkg/
│   └── utils/
│       ├── hash.go              # 哈希工具
│       ├── time.go              # 时间工具
│       └── bytes.go             # 字节工具
├── test/
│   ├── integration/             # 集成测试
│   └── benchmark/               # 性能测试
├── scripts/
│   ├── proto-gen.sh             # Protobuf 生成脚本
│   └── test.sh                  # 测试脚本
├── configs/
│   └── tokmesh.yaml             # 示例配置
├── go.mod
├── go.sum
├── Makefile
└── README.md
```

### 3. 基础工具函数

#### 3.1 哈希工具
```go
// pkg/utils/hash.go
package utils

import "hash/fnv"

// Hash64 计算字符串的 FNV-1a 哈希（用于分片路由）
func Hash64(s string) uint64 {
    h := fnv.New64a()
    h.Write([]byte(s))
    return h.Sum64()
}

// ShardIndex 计算分片索引
func ShardIndex(key string, shardCount uint64) uint64 {
    return Hash64(key) % shardCount
}
```

#### 3.2 时间工具
```go
// pkg/utils/time.go
package utils

import "time"

// NowNano 返回当前 Unix 纳秒时间戳
func NowNano() int64 {
    return time.Now().UnixNano()
}

// NowUnix 返回当前 Unix 秒时间戳
func NowUnix() int64 {
    return time.Now().Unix()
}

// AddTTL 计算过期时间（当前时间 + TTL）
func AddTTL(ttl time.Duration) int64 {
    return time.Now().Add(ttl).UnixNano()
}

// IsExpired 检查是否过期
func IsExpired(expiresAt int64) bool {
    return expiresAt > 0 && time.Now().UnixNano() > expiresAt
}
```

#### 3.3 字节工具
```go
// pkg/utils/bytes.go
package utils

// EstimateSize 估算对象序列化后的大小（用于内存统计）
func EstimateSessionSize(s *types.Session) int {
    size := 200 // 固定字段
    size += len(s.SessionID)
    size += len(s.UserID)
    size += len(s.ClientIP)
    size += len(s.UserAgent)
    // ... metadata 估算
    return size
}
```

### 4. Protobuf 编译环境

#### 4.1 安装 protoc
```bash
# scripts/install-protoc.sh
#!/bin/bash
set -e

PROTOC_VERSION=24.4
curl -LO https://github.com/protocolbuffers/protobuf/releases/download/v${PROTOC_VERSION}/protoc-${PROTOC_VERSION}-linux-x86_64.zip
unzip protoc-${PROTOC_VERSION}-linux-x86_64.zip -d /usr/local
go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
```

#### 4.2 生成脚本
```bash
# scripts/proto-gen.sh
#!/bin/bash
set -e

PROTO_DIR="internal/storage/proto"
OUT_DIR="internal/storage/proto"

protoc \
    --proto_path=${PROTO_DIR} \
    --go_out=${OUT_DIR} \
    --go_opt=paths=source_relative \
    ${PROTO_DIR}/*.proto

echo "Protobuf code generated successfully"
```

### 5. Makefile

```makefile
.PHONY: all build test proto clean

all: proto build test

# 构建
build:
	go build -o bin/tokmesh cmd/tokmesh/main.go

# 测试
test:
	go test -v -race -coverprofile=coverage.out ./...

# 测试覆盖率报告
coverage:
	go tool cover -html=coverage.out -o coverage.html

# 生成 Protobuf
proto:
	bash scripts/proto-gen.sh

# 清理
clean:
	rm -rf bin/ coverage.out coverage.html

# 格式化代码
fmt:
	go fmt ./...

# 静态检查
lint:
	golangci-lint run
```

### 6. 测试框架

#### 6.1 测试辅助函数
```go
// test/testutil/helpers.go
package testutil

import (
    "testing"
    "github.com/stretchr/testify/assert"
)

// NewTestSession 创建测试用 Session
func NewTestSession(sessionID, userID string) *types.Session {
    return &types.Session{
        SessionID:    sessionID,
        UserID:       userID,
        ClientIP:     "127.0.0.1",
        DeviceType:   types.DeviceWeb,
        SessionType:  types.SessionNormal,
        Status:       types.StatusActive,
        CreatedAt:    utils.NowNano(),
        LastActiveAt: utils.NowNano(),
        ExpiresAt:    utils.AddTTL(1 * time.Hour),
    }
}

// AssertNoError 断言无错误
func AssertNoError(t *testing.T, err error) {
    if err != nil {
        t.Fatalf("unexpected error: %v", err)
    }
}
```

### 7. 示例配置文件

```yaml
# configs/tokmesh.yaml
data_dir: /var/lib/tokmesh

max_sessions: 1000000
max_tokens: 2000000
max_memory_bytes: 4294967296  # 4GB

wal:
  fsync_mode: everysec
  batch_size: 1000
  batch_timeout: 100ms
  max_file_size: 104857600  # 100MB
  max_disk_usage: 314572800  # 300MB

snapshot:
  interval: 1h
  wal_threshold: 10000

ttl:
  session:
    normal:
      ttl: 3600s
      sliding: true
      max_ttl: 86400s
    vip:
      ttl: 7200s
      sliding: true
      max_ttl: 172800s
    admin:
      ttl: 1800s
      sliding: false
  token:
    access:
      ttl: 900s
      sliding: false
    refresh:
      ttl: 604800s
      sliding: false

eviction:
  policy: ttl-first
  threshold: 0.9
  target: 0.8

disk:
  warning_threshold: 0.2
  critical_threshold: 0.1
  recovery_threshold: 0.25
  check_interval: 30s
  fallback_mode: memory
```

## 验收标准

### 功能验收
- [ ] Go Module 初始化完成
- [ ] 目录结构创建完整
- [ ] Protobuf 编译环境配置成功
- [ ] 基础工具函数实现完整
- [ ] Makefile 可正确执行 build/test/proto 命令
- [ ] 示例配置文件可正确解析

### 测试验收
- [ ] 哈希工具单元测试通过
- [ ] 时间工具单元测试通过
- [ ] 配置解析单元测试通过
- [ ] `make test` 执行成功

## 技术要点

1. **Go Module 路径**：使用项目实际的 module 路径
2. **Protobuf 版本**：使用 proto3 和 protoc-gen-go v1.31+
3. **测试框架**：使用 testify/assert 简化断言
4. **Makefile**：支持常用开发任务自动化

## 参考文档

- `specs/2-designs/DN-254901-存储引擎架构设计.md:2950-3000`（目录结构）
- Go 官方最佳实践：https://golang.org/doc/effective_go

---

*完成此任务后，进入批次 2：核心存储模块*
